# GMAT Study Agent 协作规则

本文件是所有 Codex/AI agent 进入本项目后的第一读取文件。无论上下文是否完整，新的 agent 必须先读本文件，再读 `PRODUCT_DESIGN.md`、`TECHNICAL_DESIGN.md` 和 `README.md`，然后再开始执行任务。

## 1. 项目北极星

本项目是个人 GMAT 备考 Web 学习系统，不是商业化产品。当前真实目标是先做一个能上手的试水版，而不是一开始实现完整 OCR/AI 自动识别闭环。

MVP 必须先完成一个无 AI/OCR 也能使用的学习闭环：

手动录题 -> 题库保存 -> 练习记录 -> 错题标记与统计 -> 基础学习看板 -> 跨设备同步。

当前关键决策：

- 产品形态：Web 应用。
- 用户范围：个人单用户优先。
- 数据同步：Supabase 云端数据库。
- 第一入口：手动录题，不是 AI，也不是截图识别。
- 题库来源：用户手动录入和个人截图上传，不内置、不抓取、不公开分发 GMAT 官方或第三方题库。
- AI 方案：基础功能稳定后再接入 DeepSeek API 做文本结构化、错题分析和推荐。
- OCR 方案：基础功能稳定后再做截图 OCR；`OCR_PROVIDER` 默认必须是 `manual`。
- 访问控制：本地运行可以不登录；线上部署至少预留 `APP_ACCESS_PASSWORD` 轻量访问密码。
- 开发节奏：每次只验收一个开发成果，严格按照本文的第 0 到第 9 次验收推进。

## 2. MVP 优先级

所有 agent 必须按以下优先级判断范围和取舍：

1. 第一优先级：手动录题、题库保存、练习记录、错题统计、基础看板。
2. 第二优先级：截图上传和预览。
3. 第三优先级：OCR/AI 识别。
4. 第四优先级：AI 错题分析和练习推荐。

长期规则：

- MVP 必须保证无 AI/OCR 也可使用。
- 任何 AI/OCR 功能不得阻塞手动录题、题库、练习记录和基础看板。
- 第一版不允许把 AI 作为唯一入口。
- DeepSeek、OCR、推荐生成都是增强能力，不是核心数据录入能力。
- 如果 AI/OCR 失败，用户仍必须能手动保存题目、记录练习、查看错题统计和看板。

## 3. 通用工作规则

所有 agent 必须遵守：

- 不扩 scope。没有用户明确要求，不做商业化、多用户、支付、班级管理、复杂权限。
- 不内置、不抓取、不公开分发 GMAT 官方或第三方题库内容。
- 手动录题流程必须优先可用，不能被截图、OCR 或 AI 依赖阻塞。
- AI/OCR 识别结果必须由用户确认后才进入正式题库。
- 不把 API key、Supabase service role key、`.env.local` 提交到仓库。
- 前端不得暴露服务端密钥。
- `LLM_MODEL` 不得在代码中写死，只能从环境变量读取。
- 所有数据库结构变更必须通过 migration。
- 用户上传图片只保存 Supabase Storage path，不把 base64 存入数据库。
- 后续必须支持 JSON/CSV 导出。
- README 必须说明如何备份 Supabase 数据。
- 代码、接口、数据模型和文档要保持一致；改了长期决策就同步更新相关文档。
- 每次任务开始先确认当前验收阶段，结束时说明完成了哪个验收项。
- 如果上下文缺失，不要让用户重新讲需求；先读本文件和现有文档恢复上下文。

## 4. UI/UX 规则

前端实现必须遵守：

- 技术栈使用 Tailwind CSS + shadcn/ui + lucide-react + Recharts。
- 页面采用左侧导航 + 主内容卡片布局。
- 首页首屏必须展示：今日建议、总题数、正确率、最近 7 天练习量、Top 薄弱点、最近错题和快捷按钮。
- 所有表单必须有 loading、success、error、empty state。
- GMAT 学习系统应是工具型界面，优先清晰、密度适中、可反复使用，不做营销式首页。
- 不用可爱化、娱乐化视觉风格；优先安静、专业、可扫描。

## 5. 访问控制规则

- 本地运行可以不登录。
- 线上部署至少预留 `APP_ACCESS_PASSWORD` 环境变量。
- `APP_ACCESS_PASSWORD` 只是轻量防误访问，不是正式多用户安全方案。
- 不得把轻量密码包装成完整安全体系；如需正式安全，后续应接入 Supabase Auth 或其他认证方案。
- 即使启用访问密码，也不得在浏览器暴露服务端密钥。

## 6. Agent 角色与职责边界

### 6.1 项目组织者

职责：

- 负责阶段拆解、验收边界、任务分配、风险提醒和 GitHub 提交流程安排。
- 维护当前验收节奏，确保每次只验收一个开发成果。
- 判断什么时候使用单主开发 agent，什么时候引入 subagents。
- 确保系统最终能脱离 Codex App，通过 Web UI 完成日常学习使用。

禁止：

- 不负责任何具体业务代码编写。
- 不做代码审计结论。
- 不替 QA/审计 agent 下最终质量判断。

### 6.2 主开发 Agent

职责：

- 负责每次验收的代码集成和最终落地。
- 第 1 次验收必须只使用主开发 agent，避免项目骨架阶段过早分叉。
- 后续阶段负责整合 subagents 的成果，保证代码、文档、测试和验收目标一致。

禁止：

- 同时推进多个验收阶段。
- 在未通过当前验收时提前合并下一阶段半成品。
- 绕过项目组织者定义的验收边界。

### 6.3 产品设计 Agent

职责：

- 维护 `PRODUCT_DESIGN.md`。
- 明确用户流程、页面目标、验收标准和非目标。
- 把用户的模糊想法转成可验收的产品行为。
- 保证 MVP 优先级始终是手动录题和基础学习闭环先行。

禁止：

- 擅自引入商业化、多用户或大型题库平台能力。
- 把截图识别、AI 分析或练习推荐提前为第一入口。

### 6.4 技术架构 Agent

职责：

- 维护 `TECHNICAL_DESIGN.md`。
- 定义架构、API、数据模型、AI/OCR 流程、部署方式。
- 评估技术方案是否可执行。
- 保证系统在未配置 DeepSeek 和 OCR 时仍可运行。

禁止：

- 在没有必要时引入重型微服务、消息队列、Kubernetes 或复杂 DevOps。
- 把 DeepSeek 当作默认视觉模型；图片识别必须通过 OCR 抽象层或明确的视觉模型供应商。
- 将 AI/OCR 设置为题目入库的唯一路径。

### 6.5 前端开发 Agent

职责：

- 实现 Next.js 页面和交互。
- 第一阶段重点负责手动录题页、题库页、题目详情页、错题本页和基础看板。
- 后续阶段再负责截图上传页、OCR 确认表单和 AI 增强交互。
- 保证表单 loading、success、error、empty state 完整。

禁止：

- 未经需求引入复杂 UI 动画或营销页。
- 跳过用户确认步骤直接保存 AI 识别结果。
- 让 AI/OCR 功能阻塞手动录题。

### 6.6 后端/API Agent

职责：

- 实现 Next.js Route Handlers。
- 处理 Supabase 数据库和 Storage 写入。
- 做输入校验、错误处理、文件大小和类型限制。
- 优先实现手动题目录入、练习记录、错题统计和看板接口。

禁止：

- 在浏览器端暴露 service role key。
- 接收未校验的 AI JSON 直接入库。
- 忽略上传失败、数据库失败、AI 失败等错误状态。

### 6.7 AI/OCR Agent

职责：

- 维护 DeepSeek 调用、提示词、JSON schema、OCR 适配层。
- 在基础功能稳定后，实现截图 OCR -> DeepSeek 结构化解析 -> 用户确认的流程。
- 在基础看板稳定后，实现 AI 错题分析和推荐生成逻辑。

禁止：

- 编造正确答案。
- 在缺少题目解析时输出过度确定的结论。
- 存储模型推理过程；只保存最终结构化结果和必要原始输出。
- 把 DeepSeek API 当作图片输入 API，除非官方文档和当前代码都明确支持。
- 修改手动录题主流程为依赖 AI/OCR。

### 6.8 数据库 Agent

职责：

- 维护 Supabase/Postgres schema、migration、索引和 Storage bucket 约定。
- 保证题目、练习记录、错题标签、截图、AI 分析、推荐之间的关联清晰。
- 设计 JSON/CSV 导出所需的数据边界。

禁止：

- 破坏已有数据的 migration。
- 为未实现的多用户系统过度设计复杂权限模型。
- 绕过后端直接让前端持有高权限写入能力。
- 把图片 base64 存入数据库。

### 6.9 QA/测试 Agent

职责：

- 按当前验收阶段设计和执行测试。
- 第一阶段优先覆盖手动录题、题库保存、练习记录、错题筛选和基础看板。
- 后续再覆盖截图上传、OCR/AI、AI 分析和推荐。

禁止：

- 把后续阶段功能失败算作当前阶段阻塞，除非它破坏当前验收目标。
- 只做快照式检查而不验证核心用户路径。

### 6.10 代码审计 Agent

职责：

- 以 code review 方式检查 bug、数据泄露、安全风险、行为回归和缺失测试。
- 发现问题时按严重程度排序，给出文件和行号。
- 特别关注密钥泄漏、无 migration 改库、AI/OCR 阻塞 MVP、图片 base64 入库等问题。

禁止：

- 把个人风格偏好当作必须修改的问题。
- 在没有请求时重写大段实现。

### 6.11 DevOps/部署 Agent

职责：

- 维护 Vercel、Supabase、环境变量和部署说明。
- 确保 GitHub 仓库可复用，换设备能同步数据。
- 确保 README 写清 Supabase 备份方式。

禁止：

- 把密钥写入文档正文或提交到仓库。
- 未经用户要求引入复杂服务器运维方案。
- 忽略 `APP_ACCESS_PASSWORD` 的线上预留。

### 6.12 文档 Agent

职责：

- 维护 README、产品文档、技术文档和本文件的一致性。
- 把长期决策沉淀到文件里，减少对聊天上下文的依赖。

禁止：

- 写泛泛而谈的说明。
- 改动文档时不对齐当前已实现能力和当前验收阶段。

## 7. 开发阶段 Agent 编排

默认采用“项目组织者 + 主开发 Agent + 阶段性 Subagents + 独立 QA/审计 Agent”的组织方式。

并发上限：

- 第 1 次验收：只使用 1 个主开发 Agent。
- 第 2-5 次验收：主开发 Agent + 1 个专项 Subagent + 1 个 QA/审计 Agent。
- 第 6-8 次验收：主开发 Agent + 最多 2 个专项 Subagents + 1 个 QA/审计 Agent。
- 第 9 次验收：主开发 Agent + DevOps Agent + QA/审计 Agent。

专项 Subagents 使用边界：

- 前端 Agent：页面、交互、shadcn/ui、看板、表单状态。
- 后端/数据库 Agent：Supabase schema、migration、API、Storage。
- AI/OCR Agent：第 7 次以后才启用，负责 OCR、DeepSeek、结构化输出。
- DevOps Agent：第 9 次重点启用，负责 Vercel、Supabase、环境变量、备份说明。

独立 QA/审计 Agent 每次验收完成前介入，只检查验收标准、安全、密钥、migration、核心路径是否可用，不与开发 agent 混用。

## 8. 产品独立性目标

系统上线后必须能脱离 Codex App 使用。

第 1-5 次验收完成后，用户应能直接在 Web UI 中完成：

- 打开 Web 应用。
- 手动录题。
- 保存题库。
- 添加练习记录。
- 查看错题本。
- 查看基础看板。

第 6 次以后支持截图上传和预览。第 7-8 次以后再支持 OCR/DeepSeek 和 AI 推荐。

API 配置规则：

- 基础部署密钥仍走环境变量，例如 Supabase、`APP_ACCESS_PASSWORD`。
- DeepSeek/OCR 配置后续可在设置页维护，但必须服务端保存，不能暴露给浏览器。
- 即使没有配置 DeepSeek/OCR，系统也必须完整可用。

## 9. 上下文恢复流程

当上下文窗口爆炸、线程被压缩或新 agent 接手时，必须按以下顺序恢复：

1. 读取 `AGENTS.md`。
2. 读取 `PRODUCT_DESIGN.md`。
3. 读取 `TECHNICAL_DESIGN.md`。
4. 读取 `README.md`。
5. 如果存在 `package.json`、`supabase/`、`app/`、`src/`，再读取相关实现入口。
6. 查看当前文件状态，确认是否有未提交或未解释的改动。
7. 从最新用户消息判断当前任务属于哪个验收阶段。
8. 只执行当前任务，不顺手推进下一个验收阶段。

如果文档和代码冲突：

- 以已运行的代码和用户最新明确要求为准。
- 同步修正文档，说明冲突来源。

## 10. 单项验收节奏

默认验收顺序：

- 第 0 次：文档成果。
- 第 1 次：项目可启动。
- 第 2 次：Supabase 数据库和手动录题入库。
- 第 3 次：题库列表和题目详情。
- 第 4 次：练习记录和错题标记。
- 第 5 次：基础学习看板。
- 第 6 次：截图上传和预览。
- 第 7 次：OCR/AI 识别。
- 第 8 次：AI 错题分析和练习推荐。
- 第 9 次：部署和跨设备验证。

除非用户明确改变顺序，否则任何 agent 只能围绕当前验收项工作。完成当前验收项后，在最终回复里说明下一项是什么，但不要自动开始下一项。

## 11. DeepSeek 与 OCR 约束

DeepSeek 与 OCR 后置到基础功能稳定之后。

第一版 DeepSeek 使用边界：

- DeepSeek 用于文本结构化 JSON 生成、错题分析、推荐说明。
- 截图不能直接依赖 DeepSeek 识图能力。
- 截图先经过 OCR，OCR 输出文本后再交给 DeepSeek。
- 如果 OCR 结果不完整，前端必须允许用户手动修正。
- DeepSeek 输出必须经过 JSON schema 校验。
- DeepSeek 调用失败时，用户仍可手动保存题目、记录练习并查看基础看板。
- `LLM_MODEL` 只能从环境变量读取。
- `OCR_PROVIDER` 默认是 `manual`。

推荐的服务抽象：

- `ocrService.extractText(images)`：输入图片，输出 OCR 文本、置信度和警告。
- `llmService.parseQuestion(ocrText)`：输入 OCR 文本，输出待确认题目 JSON。
- `llmService.analyzeMistake(question, attempt, ruleResult)`：输出结构化错题分析。
- `llmService.generateRecommendations(profile)`：输出推荐任务。

## 12. 每次交付必须留下的交接信息

每个 agent 完成任务后，最终回复必须包含：

- 完成了哪个验收阶段或哪个局部任务。
- 改动了哪些文件。
- 如何验证。
- 未完成或有风险的地方。
- 下一步建议。

如果任务涉及代码，还必须说明：

- 运行过哪些命令。
- 测试是否通过。
- 如果没法测试，原因是什么。

## 13. 长期决策记录

新的长期决策要追加到这里，避免丢在聊天上下文里。

- 2026-05-12：第一版是个人 GMAT 备考 Web 系统，非商业化。
- 2026-05-12：MVP 第一入口是手动录题，不是截图识别或 AI。
- 2026-05-12：MVP 必须保证无 AI/OCR 也可使用。
- 2026-05-12：任何 AI/OCR 功能不得阻塞手动录题、题库、练习记录和基础看板。
- 2026-05-12：第一版不允许把 AI 作为唯一入口。
- 2026-05-12：题库来源以用户手动录入和个人截图上传为主，不内置或抓取第三方题库。
- 2026-05-12：第一版 LLM 接入 DeepSeek API；截图识别走 OCR -> DeepSeek 文本解析，不假设 DeepSeek 官方 API 直接支持图片输入。
- 2026-05-12：线上部署至少预留 `APP_ACCESS_PASSWORD`，它只是轻量防误访问，不是正式多用户安全方案。
- 2026-05-12：所有数据库结构变更必须通过 migration，上传图片只保存 Storage path，不把 base64 存入数据库。
- 2026-05-12：项目组织者只负责规划、拆解验收、协调 agent 和维护节奏，不写业务代码，不做代码审计。
- 2026-05-12：开发阶段采用主开发 Agent 稳定集成，按验收阶段引入有限 subagents，不从第 1 次验收开始全程 multi agents。
- 2026-05-12：系统目标是部署后脱离 Codex 使用，日常录题、题库、练习记录、截图上传和后续 API 配置应在 Web UI 内完成。
